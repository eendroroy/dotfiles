[ -f ~/.zsh_keybinds_local ] && source ~/.zsh_keybinds_local

# Insert "sudo " at the beginning of the line
function prepend-sudo {
  if [[ $BUFFER != "sudo "* ]]; then
    BUFFER="sudo $BUFFER"; CURSOR+=5;
  fi
}
zle -N prepend-sudo
bindkey "^[s" prepend-sudo
